version: '2'

services:
  db:
    image: nginx:1.16.0
    container_name: compose_test_nginx
    command: 'nginx -g "daemon off;"'
  alpine:
    image: alpine:3.7.3
    container_name: compose_test_alpine
